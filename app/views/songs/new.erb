<% if @song && @song.errors.any? %>
  <div>
    <strong>Hmm, something's not right. Please fix the errors:</strong>
    <ul>
      <% @song.errors.full_messages.each do |message| %>
        <li><%= message %></li>

      <% end %>
    </ul>
  </div>
<% end %>

<h1>Add a song!</h1>
<p>Post a song, whether it's your favourite, least favourite, or just the first one you can think of. If you have a link, add it too so others can enjoy the tune.</p>
<a href="/songs">Back to all songs</a>
<p></p>

<form method ="post" action="/songs" role="form">
  <div class="form-group">
    <label>Song title:</label>
    <input class="form-control" type="text" name="title" value="<%= @song && @song.title %>" placeholder="Song title">
  </div>

  <div class="form-group">
    <label>Artist:</label>
    <input class="form-control" type="text" name="artist" value="<%= @song && @song.artist %>"placeholder="Artist">
  </div>

  <div class="form-group">
    <label>URL (optional):</label>
    <input class="form-control" type="text" name="url" value="<%= @song && @song.url %>"placeholder="URL">
  </div>

  <div class="form-group">
    <label>Username:</label>
    <input class="form-control" type="text" name="username" value="<%= @song && @song.username %>" placeholder="Username">
  </div>

  <div>
    <button type="submit" class="btn btn-default">Submit!</button>
  </div>

</form>
